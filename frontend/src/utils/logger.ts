// Re-export from the logging-middleware package
import { Logger, LogLevel, Stack, FrontendPackage } from "logging-middleware";

// Export everything needed for the frontend
export { Logger, LogLevel, Stack, FrontendPackage };

export default Logger;
